{
  "name": "login_framework",
  "version": "1.0.0",
  "description": "This is login test automation framework",
  "main": "index.js",
  "scripts": {
    "test": "cd login_framework && PLAYWRIGHT_JSON_OUTPUT_NAME=results.json npx playwright test --reporter=json,dot"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.15.0"
  }
}
